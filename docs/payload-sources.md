# Payload sources

Every schema field and enumeration in `src/schemas/` traces back to the official RevenueCat documentation. Status is `VERIFIED` (read from the live docs on the given date) or `PROVISIONAL` (docs unreachable; validated against memory/samples — needs a validation ticket).

_Skeleton — filled in by T-002._
